import type { FC } from 'react';

export const Header: FC = () => (
  <header className="text-left flex-grow-0">
    <h1 className="font-extralight text-xl">
      ⚔️ Dream Duels: The Battle for Oneiron
    </h1>
  </header>
);
